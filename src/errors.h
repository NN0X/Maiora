#ifndef ERRORS_H
#define ERRORS_H

typedef enum PreprocessorErrors
{
        PRE_ERR_UNKNOWN = -1,
} PrErr_t;

typedef enum LexerErrors
{
        LEX_ERR_UNKNOWN = -1,
} LErr_t;

typedef enum ParserErrors
{
        PAR_ERR_UNKNOWN = -1,
} PaErr_t;

#endif
